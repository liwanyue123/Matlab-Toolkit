addpath(genpath("../Utils"));%矩阵相关

F_A = [0; 0; 0; 4; 0; 0];% force旋量[w v]
p_A = [0; 2; 0]; % force位置

% target positon
p_B=[0;0;0];
r_AB=p_B-p_A;%force arm

showType = 'all';
rate = 0.5;

F_B=calScrewFromOtherScrew(F_A,r_01)
hold on
drawVector(p_B, r_01)
showScrew(p_A,F_A ,showType,rate,'blue')
showScrew(p_B,F_B ,showType,rate,'red')

% 设置相机的角度
azimuth = 45; % 方位角
elevation = 30; % 仰角
view(azimuth, elevation); % 设置相机角度

axis equal